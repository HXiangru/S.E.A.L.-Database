CREATE TABLE uncertainty(
	uncertainty_specimen text,
	uncertainty_bone text,
	uncertainty_sex text,
	uncertainty_age text,
	uncertainty_side_of_body text,
	uncertainty_plane_of_picture text,
	uncertainty_orientation text
)